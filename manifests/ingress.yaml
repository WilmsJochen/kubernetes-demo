apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: DO_NOT_APPLY_THIS_INGRESS
spec:
  rules:
    - http:
        paths:
          - path: /YOUR_NAME
            backend:
              serviceName: YOUR_NAME-service
              servicePort: YOUR_NAME-http